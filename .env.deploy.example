# PrintForProfitable deploy configuration
# Copy this to .env.deploy (gitignored) and fill in your values

REMOTE_HOST=192.168.2.13
REMOTE_USER=root
# REMOTE_PASSWORD=your_password_here   # only needed if no SSH key is set up
REMOTE_DIR=/opt/printforprofitable

# Optional: override docker image env vars on the remote server
# DB_USER=pfp
# DB_PASSWORD=pfp_secret_2024
# JWT_SECRET=change-me-in-production
# SESSION_SECRET=change-me-in-production
